# Database Configuration
POSTGRES_USER=eepy_admin
POSTGRES_PASSWORD=changeme_securely
POSTGRES_DB=eepy_host

# Backend Configuration
BACKEND_PORT=8000
SECRET_KEY=generate_a_long_random_string_here
DATABASE_URL=postgresql://eepy_admin:changeme_securely@db:5432/eepy_host

# Frontend Configuration
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:8000

# MCP Credential Encryption (Fernet)
# Generate your own with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MCP_ENCRYPTION_KEY=generate-a-valid-fernet-key-here
